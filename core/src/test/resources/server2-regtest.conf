include "application"

bytabit.fiat-trader {
  config = "server2-regtest"

  wallet {
    net = "org.bitcoin.regtest"
  }

  server {

    enabled = true

    local-port = 9002
    public-port = 9002
  }
}
