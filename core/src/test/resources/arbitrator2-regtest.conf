include "application"

bytabit.fiat-trader {
  config = "arbitrator2-regtest"

  wallet {
    net = "org.bitcoin.regtest"
  }

  arbitrator {

    enabled = true

    bond-percent = 0.12
    btc-arbitrator-fee = 0.22
  }

  server {

    enabled = false

    local-port = 9002
    public-port = 9002
  }
}