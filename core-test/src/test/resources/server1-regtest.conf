include "application"

bytabit.fiat-trader {
  config = "server1-regtest"

  wallet {
    net = "org.bitcoin.regtest"
  }

  server {

    enabled = true

    local-port = 9001
    public-port = 9001
  }
}
