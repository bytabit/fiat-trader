include "application"

bytabit.fiat-trader {
  config = "arbitrator1-regtest"

  wallet {
    net = "org.bitcoin.regtest"
  }

  arbitrator {

    enabled = true

    bond-percent = 0.10
    btc-arbitrator-fee = 0.20
  }

  server {

    enabled = false

    local-port = 9001
    public-port = 9001
  }
}