include "application"

bytabit.fiat-trader {
  config = "arbitrator2"

  wallet {
    net = "org.bitcoin.regtest"
  }

  arbitrator-server {

    enabled = true

    bond-percent = 0.10
    btc-arbitrator-fee = 0.20

    local-port = 9002
    public-port = 9002
  }
}