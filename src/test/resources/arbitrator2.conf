include "application"

bytabit.fiat-trader {
  config = "arbitrator2"

  wallet {
    net = "org.bitcoin.regtest"
  }

  arbitrator {

    enabled = true

    bond-percent = 0.10
    btc-arbitrator-fee = 0.20
  }

  server {

    enabled = false

    local-port = 9002
    public-port = 9002
  }
}