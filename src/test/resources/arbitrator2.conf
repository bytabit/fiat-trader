include "application"

bytabit.fiat-trader {
  config = "arbitrator2"

  wallet {
    net = "org.bitcoin.regtest"
  }

  notary-server {

    enabled = true

    bond-percent = 0.10
    btc-notary-fee = 0.20

    local-port = 9002
    public-port = 9002
  }
}