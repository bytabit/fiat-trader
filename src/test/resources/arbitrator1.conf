include "application"

bytabit.fiat-trader {
  config = "arbitrator1"

  wallet {
    net = "org.bitcoin.regtest"
  }

  arbitrator {

    enabled = true
  }

  server {

    enabled = false

    local-port = 9001
    public-port = 9001
  }
}
