include "application"

bytabit.fiat-trader {
  config = "arbitrator1"

  wallet {
    net = "org.bitcoin.regtest"
  }

  notary-server {

    enabled = true

    local-port = 9001
    public-port = 9001
  }
}
